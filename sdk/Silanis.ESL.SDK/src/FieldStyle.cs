using System;

namespace Silanis.ESL.SDK
{
	public enum FieldStyle
	{
        BOUND_DATE,
		BOUND_NAME,
		BOUND_TITLE,
		BOUND_COMPANY,
        BOUND_QRCODE,
		UNBOUND_TEXT_FIELD,
		UNBOUND_CUSTOM_FIELD,
		UNBOUND_CHECK_BOX,
        UNBOUND_RADIO_BUTTON,
		LABEL
	}
}